# Распознавание рукописных цифр на Java

Это учебный проект по созданию и обучению **Сверточной Нейронной Сети (CNN)** для классификации рукописных цифр. Проект реализован на языке Java с использованием библиотеки **Deeplearning4j (DL4J)**.

## 🚀 Возможности
* **Deep Learning архитектура**: Использование CNN (Convolutional Neural Network) для высокой точности.
* **Умная предобработка**: Автоматическая инверсия цветов, бинаризация и центрирование цифры в кадре.
* **Универсальный предсказатель**: Возможность анализировать как одиночные файлы, так и пакеты изображений в папке.
* **Промышленный подход**: Код разделен на модули, магические числа вынесены в константы.

---

## 🏗 Архитектура нейросети
Модель построена на базе классической архитектуры LeNet-5:
1. **Input**: Изображение 28x28 пикселей (1 канал).
2. **Convolution Layer**: 20 фильтров (5x5) для выделения базовых признаков.
3. **Pooling Layer**: Max Pooling (2x2) для уменьшения размерности.
4. **Convolution Layer**: 50 фильтров (5x5) для сложных форм.
5. **Dense Layer**: Полносвязный слой на 500 нейронов.
6. **Output Layer**: Softmax классификатор на 10 классов (цифры 0-9).



---

## 🛠 Инструкция по запуску

### 1. Требования
* JDK 11 или выше.
* Maven для управления зависимостями.
* Установленные зависимости `deeplearning4j-core` и `nd4j-native-platform`.

### 2. Обучение модели
Запустите класс `MnistTrainer.java`.
* Программа загрузит датасет MNIST (60,000 изображений).
* Проведет обучение (по умолчанию 5 эпох).
* Сохранит готовую модель в файл `trained_mnist_model.zip`.

### 3. Распознавание
Поместите свои `.png` файлы в папку `src/main/resources/test_images` и запустите `Predictor.java`.
Размер и цвета не должны повлиять на результат, но уверенность модели будет ниже.

Пример вывода:
```text
Файл                 | Цифра      | Уверенность
----------------------------------------------------
digit_7.png          | 7          | 99.85%
my_test.png          | 3          | 94.20%
